pub struct Tokens {
    pub bg: u32,
    pub surface: u32,
    pub fg: u32,
    pub muted: u32,
    pub accent: u32,
}

pub const TOKENS: Tokens = Tokens {
    bg: 0x1e1e2e,
    surface: 0x282838,
    fg: 0xeeeeee,
    muted: 0x9999aa,
    accent: 0x7aa2f7,
};
